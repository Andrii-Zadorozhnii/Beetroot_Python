print('\n1. Потоки (threading)\n')
#
# Потоки полезны, когда программа выполняет много операций ввода-вывода (I/O), таких как работа с сетью, файловыми операциями или запросами к базе данных.
#
# 🔹 Преимущества:
# 	•	Простота использования.
# 	•	Экономия памяти по сравнению с процессами.
# 	•	Хорошо подходит для задач с I/O (сетевые запросы, работа с файлами).
#
# 🔹 Недостатки:
# 	•	GIL (Global Interpreter Lock) – только один поток может выполнять Python-код в один момент времени.
# 	•	Неэффективен для задач, требующих большого количества вычислений.
#

import threading
import time


def task(name):
    print(f"Запуск {name}")
    time.sleep(2)
    print(f"Завершение {name}")


threads = []
for i in range(3):
    thread = threading.Thread(target=task, args=(f"Поток {i}",))
    threads.append(thread)
    thread.start()

for thread in threads:
    thread.join()

print("Все потоки завершены")

print('\n2. Процессы (multiprocessing)\n')
#
# Процессы работают параллельно и могут использовать разные процессорные ядра. Это позволяет выполнять вычислительно затратные операции намного быстрее, чем при использовании потоков.
#
# 🔹 Преимущества:
# 	•	Нет проблем с GIL – каждый процесс имеет свой Python-интерпретатор.
# 	•	Поддержка многопроцессорных архитектур.
#
# 🔹 Недостатки:
# 	•	Большие затраты на создание процессов по сравнению с потоками.
# 	•	Более сложное взаимодействие между процессами (используются очереди, пайпы и shared memory).
#
# 📌 Пример использования процессов:

import multiprocessing
import time


def task(name):
    print(f"Запуск {name}")
    time.sleep(2)
    print(f"Завершение {name}")


processes = []
for i in range(3):
    process = multiprocessing.Process(target=task, args=(f"Процесс {i}",))
    processes.append(process)
    process.start()

for process in processes:
    process.join()

print("Все процессы завершены")

print('\n3. Асинхронность (asyncio)\n')
#
# Асинхронный подход использует корутины и событийный цикл для выполнения задач без блокировки основного потока. Это похоже на потоки, но без создания дополнительных потоков или процессов.
#
# 🔹 Преимущества:
# 	•	Эффективное выполнение I/O-операций без блокировки.
# 	•	Хорошо масштабируется.
# 	•	Легче управлять, чем потоками и процессами.
#
# 🔹 Недостатки:
# 	•	Не подходит для CPU-интенсивных задач.
# 	•	Требует поддержки асинхронных библиотек (например, aiohttp для HTTP-запросов).
#
# 📌 Пример использования asyncio:

import asyncio


async def task(name):
    print(f"Запуск {name}")
    await asyncio.sleep(2)
    print(f"Завершение {name}")


async def main():
    tasks = [task(f"Задача {i}") for i in range(3)]
    await asyncio.gather(*tasks)


asyncio.run(main())
